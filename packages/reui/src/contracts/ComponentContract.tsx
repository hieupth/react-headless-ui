/**
 * Alias for the focusable behavior props. Hooks that compose focus behavior
 * extend this interface (kept here for backward-compatible imports).
 */
export type { FocusableMixinProps as FocusableProps } from '../mixins/FocusableMixin';
